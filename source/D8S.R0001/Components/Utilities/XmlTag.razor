@*Namespace specified in directory-level _Imports.razor.*@

@*Wrapped in a span so that the Razor engine removes leading and trailing whitespace.*@
<span><AngleBrackets>@this.TagName</AngleBrackets>@this.ChildContent<AngleBrackets>@this.TagName</AngleBrackets></span>


@code{
    /// <summary>
    /// The name of the tag.
    /// </summary>
    [Parameter]
    public string TagName { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}
